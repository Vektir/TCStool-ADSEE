This app launches a locally hosted website that displays a table, where initial parameters for the telecommunications system of a spacecraft are entered, and then a new table is outputted which displays the signal over noise ratio as well as other important factors. 
To run this app install dependencies with 
pip install -r requirements.txt
Then run the Dash.py file